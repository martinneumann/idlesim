import p5 from "p5";

export class graphics {
	constructor() {

		// Create a new canvas for a div named "gameworld"
		/*
		p5.setup = (width: number, height: number) => {
			p5.createCanvas(100, 50);
			p5.background(153);
			p5.line(0, 0, width, height);
		}
		*/
	}




}